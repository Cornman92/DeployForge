# DeployForge - Core Dependencies
# Pinned versions for reproducible builds
# Last updated: 2025-11-28

# CLI and UI
click==8.1.7
rich==13.7.0
PyQt6==6.6.1

# Data formats
pyyaml==6.0.1
xmltodict==0.13.0
pycdlib==1.14.0

# API and web
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# System utilities
psutil==5.9.7
requests==2.32.4

# Platform-specific dependencies
# Windows: None (uses built-in DISM)
# Linux: Requires wimlib-imagex (system package)
# macOS: Requires wimlib (brew install wimlib)
